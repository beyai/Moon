///
/// HybridQRScanerComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#import "HybridQRScanerComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridQRScanerSpecSwift.hpp"
#import "NitroMoon-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::nitromoon;
using namespace margelo::nitro::nitromoon::views;

/**
 * Represents the React Native View holder for the Nitro "QRScaner" HybridView.
 */
@interface HybridQRScanerComponent: RCTViewComponentView
@end

@implementation HybridQRScanerComponent {
  std::shared_ptr<HybridQRScanerSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridQRScanerComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridQRScanerComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridQRScanerSpec> hybridView = NitroMoon::NitroMoonAutolinking::createQRScaner();
    _hybridView = std::dynamic_pointer_cast<HybridQRScanerSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroMoon::HybridQRScanerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridQRScanerProps const>(props);
  auto& newViewProps = const_cast<HybridQRScanerProps&>(newViewPropsConst);
  NitroMoon::HybridQRScanerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // isActive: boolean
  if (newViewProps.isActive.isDirty) {
    swiftPart.setIsActive(newViewProps.isActive.value);
    newViewProps.isActive.isDirty = false;
  }
  // paused: boolean
  if (newViewProps.paused.isDirty) {
    swiftPart.setPaused(newViewProps.paused.value);
    newViewProps.paused.isDirty = false;
  }
  // onScan: optional
  if (newViewProps.onScan.isDirty) {
    swiftPart.setOnScan(newViewProps.onScan.value);
    newViewProps.onScan.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
