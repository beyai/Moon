///
/// SignalState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitromoon {

  /**
   * An enum which can be represented as a JavaScript union (SignalState).
   */
  enum class SignalState {
    CONNECTING      SWIFT_NAME(connecting) = 0,
    CONNECTED      SWIFT_NAME(connected) = 1,
    DISCONNECTED      SWIFT_NAME(disconnected) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitromoon

namespace margelo::nitro {

  // C++ SignalState <> JS SignalState (union)
  template <>
  struct JSIConverter<margelo::nitro::nitromoon::SignalState> final {
    static inline margelo::nitro::nitromoon::SignalState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("connecting"): return margelo::nitro::nitromoon::SignalState::CONNECTING;
        case hashString("connected"): return margelo::nitro::nitromoon::SignalState::CONNECTED;
        case hashString("disconnected"): return margelo::nitro::nitromoon::SignalState::DISCONNECTED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum SignalState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitromoon::SignalState arg) {
      switch (arg) {
        case margelo::nitro::nitromoon::SignalState::CONNECTING: return JSIConverter<std::string>::toJSI(runtime, "connecting");
        case margelo::nitro::nitromoon::SignalState::CONNECTED: return JSIConverter<std::string>::toJSI(runtime, "connected");
        case margelo::nitro::nitromoon::SignalState::DISCONNECTED: return JSIConverter<std::string>::toJSI(runtime, "disconnected");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert SignalState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("connecting"):
        case hashString("connected"):
        case hashString("disconnected"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
