///
/// HybridQRScanerComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#include "HybridQRScanerComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitromoon::views {

  extern const char HybridQRScanerComponentName[] = "QRScaner";

  HybridQRScanerProps::HybridQRScanerProps(const react::PropsParserContext& context,
                                           const HybridQRScanerProps& sourceProps,
                                           const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    isActive([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("isActive", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.isActive;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.isActive);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("QRScaner.isActive: ") + exc.what());
      }
    }()),
    paused([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("paused", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.paused;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.paused);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("QRScaner.paused: ") + exc.what());
      }
    }()),
    onScan([&]() -> CachedProp<std::optional<std::function<void(const std::string& /* result */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onScan", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onScan;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::string& /* result */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onScan);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("QRScaner.onScan: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridQRScanerSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridQRScanerSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("QRScaner.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridQRScanerProps::HybridQRScanerProps(const HybridQRScanerProps& other):
    react::ViewProps(),
    isActive(other.isActive),
    paused(other.paused),
    onScan(other.onScan),
    hybridRef(other.hybridRef) { }

  bool HybridQRScanerProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("isActive"): return true;
      case hashString("paused"): return true;
      case hashString("onScan"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridQRScanerComponentDescriptor::HybridQRScanerComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridQRScanerComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                    const std::shared_ptr<const react::Props>& props,
                                                                                    react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridQRScanerShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridQRScanerComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridQRScanerShadowNode&>(shadowNode);
    const HybridQRScanerProps& props = concreteShadowNode.getConcreteProps();
    HybridQRScanerState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitromoon::views
