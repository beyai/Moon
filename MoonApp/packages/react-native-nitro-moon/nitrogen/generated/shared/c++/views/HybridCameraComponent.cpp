///
/// HybridCameraComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#include "HybridCameraComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitromoon::views {

  extern const char HybridCameraComponentName[] = "Camera";

  HybridCameraProps::HybridCameraProps(const react::PropsParserContext& context,
                                       const HybridCameraProps& sourceProps,
                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    isActive([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("isActive", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.isActive;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.isActive);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.isActive: ") + exc.what());
      }
    }()),
    preview([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("preview", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.preview;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.preview);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.preview: ") + exc.what());
      }
    }()),
    filter([&]() -> CachedProp<CameraFilter> {
      try {
        const react::RawValue* rawValue = rawProps.at("filter", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.filter;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<CameraFilter>::fromRawValue(*runtime, value, sourceProps.filter);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.filter: ") + exc.what());
      }
    }()),
    fps([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("fps", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.fps;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.fps);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.fps: ") + exc.what());
      }
    }()),
    zoom([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("zoom", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.zoom;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.zoom);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.zoom: ") + exc.what());
      }
    }()),
    focus([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("focus", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.focus;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.focus);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.focus: ") + exc.what());
      }
    }()),
    exposure([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("exposure", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.exposure;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.exposure);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.exposure: ") + exc.what());
      }
    }()),
    brightness([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("brightness", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.brightness;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.brightness);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.brightness: ") + exc.what());
      }
    }()),
    orientation([&]() -> CachedProp<CameraOrientation> {
      try {
        const react::RawValue* rawValue = rawProps.at("orientation", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.orientation;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<CameraOrientation>::fromRawValue(*runtime, value, sourceProps.orientation);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.orientation: ") + exc.what());
      }
    }()),
    onInitialized([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onInitialized", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onInitialized;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onInitialized);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.onInitialized: ") + exc.what());
      }
    }()),
    onChangeDevice([&]() -> CachedProp<std::optional<std::function<void(const CameraFormat& /* format */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onChangeDevice", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onChangeDevice;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const CameraFormat& /* format */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onChangeDevice);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.onChangeDevice: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridCameraSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridCameraSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Camera.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridCameraProps::HybridCameraProps(const HybridCameraProps& other):
    react::ViewProps(),
    isActive(other.isActive),
    preview(other.preview),
    filter(other.filter),
    fps(other.fps),
    zoom(other.zoom),
    focus(other.focus),
    exposure(other.exposure),
    brightness(other.brightness),
    orientation(other.orientation),
    onInitialized(other.onInitialized),
    onChangeDevice(other.onChangeDevice),
    hybridRef(other.hybridRef) { }

  bool HybridCameraProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("isActive"): return true;
      case hashString("preview"): return true;
      case hashString("filter"): return true;
      case hashString("fps"): return true;
      case hashString("zoom"): return true;
      case hashString("focus"): return true;
      case hashString("exposure"): return true;
      case hashString("brightness"): return true;
      case hashString("orientation"): return true;
      case hashString("onInitialized"): return true;
      case hashString("onChangeDevice"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridCameraComponentDescriptor::HybridCameraComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridCameraComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                  const std::shared_ptr<const react::Props>& props,
                                                                                  react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridCameraShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridCameraComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridCameraShadowNode&>(shadowNode);
    const HybridCameraProps& props = concreteShadowNode.getConcreteProps();
    HybridCameraState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitromoon::views
