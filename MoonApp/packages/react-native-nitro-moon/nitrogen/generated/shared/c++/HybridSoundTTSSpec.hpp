///
/// HybridSoundTTSSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Voice` to properly resolve imports.
namespace margelo::nitro::nitromoon { struct Voice; }
// Forward declaration of `SpeakOptions` to properly resolve imports.
namespace margelo::nitro::nitromoon { struct SpeakOptions; }

#include "Voice.hpp"
#include <vector>
#include <string>
#include "SpeakOptions.hpp"
#include <optional>

namespace margelo::nitro::nitromoon {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SoundTTS`
   * Inherit this class to create instances of `HybridSoundTTSSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSoundTTS: public HybridSoundTTSSpec {
   * public:
   *   HybridSoundTTS(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSoundTTSSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSoundTTSSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSoundTTSSpec() override = default;

    public:
      // Properties
      virtual std::vector<Voice> getVoices() = 0;
      virtual Voice getVoice() = 0;
      virtual double getRateMinValue() = 0;
      virtual double getRateMaxValue() = 0;
      virtual double getPitchMinValue() = 0;
      virtual double getPitchMaxValue() = 0;

    public:
      // Methods
      virtual void setVoice(const std::string& identifier) = 0;
      virtual void setRate(double rate) = 0;
      virtual void setPitch(double pitch) = 0;
      virtual void speak(const std::string& text, const std::optional<SpeakOptions>& options) = 0;
      virtual void stop(std::optional<bool> onWordBoundary) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SoundTTS";
  };

} // namespace margelo::nitro::nitromoon
