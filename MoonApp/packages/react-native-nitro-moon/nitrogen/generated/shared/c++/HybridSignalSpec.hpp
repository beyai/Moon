///
/// HybridSignalSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `SignalRequest` to properly resolve imports.
namespace margelo::nitro::nitromoon { struct SignalRequest; }
// Forward declaration of `SignalState` to properly resolve imports.
namespace margelo::nitro::nitromoon { enum class SignalState; }
// Forward declaration of `SignalResponse` to properly resolve imports.
namespace margelo::nitro::nitromoon { struct SignalResponse; }

#include <NitroModules/Promise.hpp>
#include "SignalRequest.hpp"
#include <functional>
#include "SignalState.hpp"
#include "SignalResponse.hpp"
#include <string>

namespace margelo::nitro::nitromoon {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Signal`
   * Inherit this class to create instances of `HybridSignalSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSignal: public HybridSignalSpec {
   * public:
   *   HybridSignal(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSignalSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSignalSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSignalSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> open() = 0;
      virtual void close() = 0;
      virtual std::shared_ptr<Promise<void>> send(const SignalRequest& message) = 0;
      virtual std::function<void()> onStatus(const std::function<void(SignalState /* state */)>& listener) = 0;
      virtual std::function<void()> onResponse(const std::function<void(const SignalResponse& /* data */)>& listener) = 0;
      virtual std::function<void()> onError(const std::function<void(const std::string& /* error */)>& listener) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Signal";
  };

} // namespace margelo::nitro::nitromoon
